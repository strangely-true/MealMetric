<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Recipe - MealMetric</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/your-fontawesome-kit.js" crossorigin="anonymous"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Poppins', 'sans-serif'],
                    },
                    colors: {
                        'primary': '#4CAF50',
                        'primary-dark': '#45a049',
                        'secondary': '#8BC34A',
                    }
                }
            }
        }
    </script>
</head>
<body class="font-sans bg-gradient-to-br from-green-50 to-green-100 min-h-screen flex flex-col">
    <nav class="bg-primary shadow-md w-full z-10 transition-all duration-300">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <a href="/index" class="text-2xl font-bold text-white hover:text-green-200 transition duration-300">
                    <i class="fas fa-leaf mr-2"></i>MealMetric.
                </a>
                <button onclick="location.href='/logout'" class="bg-white text-primary hover:bg-green-100 font-bold py-2 px-4 rounded-full transition duration-300 transform hover:scale-105">
                    Logout
                </button>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-4 py-8 flex-grow">
        <h1 class="text-3xl font-bold text-gray-800 mb-6">Create New Recipe</h1>
        
        <form id="newRecipeForm" action="/recipes" method="POST" class="bg-white rounded-lg shadow-lg p-6">
            <div class="mb-4">
                <label for="recipeName" class="block text-gray-700 text-sm font-bold mb-2">Recipe Name</label>
                <input type="text" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary" id="recipeName" name="recipeName" required>
            </div>
            <div class="mb-4">
                <label for="instructions" class="block text-gray-700 text-sm font-bold mb-2">Recipe Instructions</label>
                <textarea class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary" id="instructions" name="instructions" rows="4"></textarea>
            </div>
            <div id="ingredientList" class="space-y-4">
                <!-- Ingredients form will be populated here -->
            </div>
            <button type="button" id="addIngredientBtn" class="mt-4 bg-secondary hover:bg-secondary-dark text-white font-bold py-2 px-4 rounded transition duration-300">
                <i class="fas fa-plus mr-2"></i>Add Ingredient
            </button>
            <button type="submit" class="mt-6 bg-primary hover:bg-primary-dark text-white font-bold py-2 px-4 rounded-full transition duration-300 transform hover:scale-105">
                Create Recipe
            </button>
        </form>
    </main>

    <footer class="bg-gray-800 text-white py-6 mt-12">
        <div class="container mx-auto px-4 text-center">
            <p class="text-sm">Powered by Nutritionix API</p>
            <p class="text-sm">&copy; 2024 MealMetric. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="/js/new_recipe.js"></script>
</body>
</html>